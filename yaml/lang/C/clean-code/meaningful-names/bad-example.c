#include <stdio.h>

// Define a class equivalent in C, typically structured as a function for simplicity.
int d(int d1, int d2) {
    // Subtract d2 from d1
    return d1 - d2;
}

int main() {
    // Example usage
    int result = d(10, 5);
    printf("Result: %d\n", result);
    return 0;
}